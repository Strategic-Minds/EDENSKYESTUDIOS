import { NextResponse } from 'next/server';

export const dynamic = 'force-dynamic';

export async function GET() {
  const html = `<!doctype html><html><head><meta name="viewport" content="width=device-width,initial-scale=1"><title>ES001 Upload</title><style>body{font-family:system-ui;margin:0;background:#111;color:#fff;padding:20px}main{max-width:680px;margin:auto;background:#1b1b1f;border:1px solid #333;border-radius:18px;padding:20px}input,button,pre{width:100%;box-sizing:border-box;margin-top:12px;padding:12px;border-radius:10px;border:1px solid #444}button{background:#fff;color:#111;font-weight:800}pre{white-space:pre-wrap;background:#08080a;color:#e8e8e8}.muted{color:#bbb}</style></head><body><main><h1>ES001 Mobile Upload Bridge</h1><p class="muted">Choose the approved ES001-01 image. The page converts it to base64 and sends it to the preview Drive upload executor.</p><input id="token" placeholder="Optional bridge token" type="password"><input id="file" type="file" accept="image/png,image/jpeg,image/webp"><button id="go">Install ES001-01 Into Drive</button><pre id="out">Waiting.</pre></main><script>const out=document.getElementById('out');document.getElementById('go').onclick=async()=>{const f=document.getElementById('file').files[0];if(!f){out.textContent='Choose an image first.';return;}out.textContent='Reading image...';const b64=await new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(String(r.result).split(',')[1]);r.onerror=rej;r.readAsDataURL(f);});out.textContent='Uploading...';const payload={assetId:'ES001-01',modelId:'ES001',modelName:'Eden Skye',shotNumber:1,shotName:'Close-Up Beauty Portrait',filename:'ESS_EDEN_SKYE_ES001_SHOT01_CLOSE_UP_BEAUTY_PORTRAIT_ULTRA_REAL_2026-06-11.png',mimeType:f.type||'image/png',approvalStatus:'approved_locked',qaStatus:'approved',usageBoundary:'private_studio_only',publicUseAllowed:false,targetFolderId:'1oCEjD6kUm9FiYDh1w-dNE9PPiggj65MQ',base64Data:b64};const token=document.getElementById('token').value;const headers={'content-type':'application/json'};if(token)headers.authorization='Bearer '+token;const response=await fetch('/api/bridge/drive-image-upload',{method:'POST',headers,body:JSON.stringify(payload)});const text=await response.text();out.textContent='HTTP '+response.status+'\n'+text;};</script></body></html>`;
  return new NextResponse(html, { headers: { 'content-type': 'text/html; charset=utf-8' } });
}
