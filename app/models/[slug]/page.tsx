import { Header, galleryAssets, models, primaryModel, standaloneAssets } from '../../visual-source-truth';
import { Header as SiteHeader } from '../../components';
export function generateStaticParams(){return models.map(model=>({slug:model.slug}));}
export default function Profile({params}:{params:{slug:string}}){const model=models.find(item=>item.slug===params.slug)||primaryModel;return <main className="eden-site"><SiteHeader/><section className="profile-shell"><img className="profile-hero-img" src={model.image} alt={`${model.name} generated standalone profile source`}/><div className="profile-copy"><h1>{model.name}</h1><p className="pink">{model.location}</p><div className="profile-stats"><span>5.2M<small>Following</small></span><span>98%<small>Engagement</small></span><span>200+<small>Brands</small></span></div><p>Confident, bold, and unapologetically authentic. Campaign-ready profile rebuilt from generated standalone Eden Skye assets.</p><a className="hot-btn" href="/apply">Apply to work with {model.name.split(' ')[0]}</a></div></section><section className="gallery-block"><h2>Photos</h2><div>{galleryAssets.map(asset=><img key={asset.id} src={asset.src} alt={asset.label}/>)}</div><h2>Videos</h2><div>{[standaloneAssets.aiVideo, standaloneAssets.aiChat, standaloneAssets.homeHero, standaloneAssets.closetFullBody].map(asset=><figure key={asset.id}><img src={asset.src} alt={asset.label}/><figcaption>Draft video packet</figcaption></figure>)}</div></section><section className="black-card-cta"><h2>Want access to Eden's Closet?</h2><a className="hot-btn" href="/pricing">Get Black Card</a></section></main>}
