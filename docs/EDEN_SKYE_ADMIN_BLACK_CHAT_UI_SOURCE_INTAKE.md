# EDEN SKYE ADMIN Black Chat UI Source Intake

## Source Package

Uploaded package: `01-black-chat-ui-2-7-.zip`

The package was unpacked and inspected as a concrete source reference for EDEN SKYE ADMIN style, layout, and autonomous-agent workflow patterns.

## Verified Source Surfaces

The package includes these usable source patterns:

- `components/workspace/workspace-shell.tsx`: three-pane command workspace with chat, tool rail, editor, and mobile tab mode.
- `components/workspace/chat-panel.tsx`: autonomous chat composer, model selector, attachment/browse actions, autonomy controls, task queue, and design-system controls.
- `components/bridge-command-center.tsx`: bridge command center with registry, action surfaces, smoke order, mutation policy, and status cards.
- `lib/autonomous-bridge-registry.ts`: bridge registry covering GPT cloud control, frontend control, generator factory, GitHub, Vercel, Supabase, agents, social, Google Chat, n8n, HeyGen, Metricool, Shopify, Stripe, and browser QA.
- `lib/approvals/gate.ts`: pause-for-approval, approval polling, workflow status updates, and approval inbox pattern.
- `lib/agent/*`: agent core, workflow orchestration, browser automation, API connectors, task executor, local operations, and safety validator patterns.
- `lib/browser/evidence.ts`: browser evidence pattern.
- `components/supabase-bridge-panel.tsx`: Supabase bridge UI pattern.
- `components/enhancements/task-queue.tsx`: task queue UI pattern.
- `components/enhancements/autonomy-slider.tsx`: guided/assisted/full autonomy control pattern.
- `components/enhancements/design-canvas.tsx`: v0-like UI/design generation surface pattern.
- `components/recursive-auto-builder-ui.tsx`: recursive AUTO BUILDER workflow surface.
- `app/api/bridge/*`: preflight, policy check, approval request, command, direct execution, Supabase switch, zero-inference, and v0 current work route patterns.

## Eden Adaptation Rule

These source files are not copied blindly into Eden Skye production. They are adapted into EDEN SKYE ADMIN as governed command-center modules with black luxury styling, human approval gates, draft-only external writes, and receipt-based evidence.

## Directly Wired Into Eden Skye Admin

- Black command-center shell under `/admin` and `/admin/*`.
- Bridge registry and protected-action policy under `/api/admin/eden/*`.
- Builder docs and manifests under `docs/` and `config/`.
- Guardrail tests under `tests/eden-skye-admin.test.mjs`.
- Draft PR #2 body updated to record install state and remaining validation gates.

## Not Directly Copied

The source package includes some unrelated Nashville Resin Worx pages and generic placeholder assets. Those are not Eden Skye source truth and must not be used for Eden Skye Admin branding.

## Remaining Evidence Gate

The unpacked source was inspected and adapted, but browser screenshots must still be captured from the actual Eden Skye app preview before visuals can be marked approved.